[project]
name = "svn-client"
version = "0.1.0"
description = "SVN CLI wrapper"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/svn_client"]
