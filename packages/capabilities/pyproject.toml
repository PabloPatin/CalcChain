[project]
name = "calcchain-capabilities"
version = "0.1.0"
description = "Shared capability contracts for CalcChain core and plugins"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/calcchain_capabilities"]
